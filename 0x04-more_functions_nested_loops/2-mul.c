#include "main.h"
 /**
  * mul - multiplies two integers
  * @a: the first number to be computed
  * @b: the second number to be computed
  * Return: Nothing!
  */

int mul(int a, int b)
{
	return (a * b);
}
